# Konduit - Read from the Linux Kernel in C++

Konduit uses Kontraband to build readable device nodes originating in the Linux kernel using C++ on both ends. These nodes are read like files and may contain byte streams or structured data.
